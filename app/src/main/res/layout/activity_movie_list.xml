<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_movie_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#01d277"
    tools:context="com.alexapps.movienight.ui.MovieListActivity">

    <android.support.v7.widget.RecyclerView
        android:layout_alignParentTop="true"
        android:paddingTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/moreMoviesButton"
        android:paddingBottom="10dp"
        android:id="@+id/recyclerView"/>

         <TextView
            android:text="@string/no_movies_found"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/emptyTextView"
            android:textSize="20sp"
            android:textColor="#ccffffff"
            android:gravity="center"
            android:layout_alignParentTop="true"
            android:layout_alignParentBottom="true"
            android:visibility="invisible"
             />

    <Button
        android:text="@string/more_movies"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/moreMoviesButton"
        android:textSize="20sp"
        android:background="#40ffffff"
        android:layout_alignParentBottom="true"
        />

</RelativeLayout>
